app: appDemo
stage: dev
component: koa
name: koaDemo
inputs:
  src:
    src: ./
    exclude:
      - .env
  functionConf:
    name: ${name}
  apigatewayConf:
    protocols:
      - http
      - https
