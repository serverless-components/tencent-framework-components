app: appDemo
stage: dev

component: express@dev
name: expressDemo

inputs:
  src:
    src: ./
    exclude:
      - .env
  functionConf:
    name: ${name}
  apigatewayConf:
    protocols:
      - http
      - https
