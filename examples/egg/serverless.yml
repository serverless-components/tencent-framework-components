app: appDemo
stage: dev
component: egg
name: eggDemo
inputs:
  src:
    src: ./
    exclude:
      - .env
  functionConf:
    name: ${name}
    namespace: test
  apigatewayConf:
    protocols:
      - http
      - https
