app: appDemo
stage: dev
component: nestjs
name: nestDemo
inputs:
  src:
    src: ./
    exclude:
      - .env
  functionConf:
    name: ${name}
  apigatewayConf:
    protocols:
      - http
      - https
